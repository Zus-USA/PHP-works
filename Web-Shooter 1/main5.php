<?php

$user = null;
$user?->getName();
//$user->getName();

//?-> - это null-safe оператор, позволяющий безопасно вызывать методы или обращаться к свойствам объекта, даже если он null. 
// Без него (используя обычный ->) при попытке вызова метода на null возникает фатальная ошибка.

?>
