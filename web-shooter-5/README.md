# Task Manager - Web Shooter 5

Современное веб-приложение для управления задачами, построенное на собственном PHP фреймворке.

## Особенности

- ✨ Современный и красивый интерфейс с темной темой
- 🎨 Градиенты и плавные анимации
- 📱 Адаптивный дизайн для всех устройств
- 🔒 Защита от CSRF атак
- ✅ Валидация данных на стороне сервера
- 💾 Поддержка файлового и MySQL хранилищ
- 🚀 Быстрая и оптимизированная работа

## Требования

- PHP 8.0 или выше
- Composer
- Веб-сервер (Apache/Nginx) или встроенный PHP сервер

## Установка

1. Установите зависимости:
```bash
composer install
```

2. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

3. Настройте `.env` файл под свои нужды:
   - `REPOSITORY_TYPE` - тип хранилища: `file` или `mysql`
   - Для MySQL настройте параметры подключения к базе данных

4. Создайте необходимые директории:
```bash
mkdir -p storage/app
```

5. Если используете MySQL, создайте таблицу:
```sql
CREATE TABLE tasks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    completed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

## Запуск

### Встроенный PHP сервер:
```bash
php -S localhost:8000 -t public
```

### Apache/Nginx:
Настройте виртуальный хост так, чтобы корневая директория указывала на папку `public`.

## Структура проекта

```
web-shooter-5/
├── app/                    # Приложение
│   ├── Http/
│   │   └── Controllers/   # Контроллеры
│   ├── Models/             # Модели
│   └── Repository/         # Репозитории
├── bootstrap/             # Загрузка приложения
├── config/                 # Конфигурация
├── public/                 # Публичная директория
│   └── index.php          # Точка входа
├── resources/              # Ресурсы
│   ├── css/               # Стили
│   ├── js/                # JavaScript
│   └── views/             # Шаблоны
├── routes/                 # Маршруты
├── src/                    # Исходный код фреймворка
│   └── Cymphone/
└── storage/                # Хранилище данных
    └── app/
```

## Использование

1. Откройте приложение в браузере
2. Нажмите "Добавить задачу" для создания новой задачи
3. Просматривайте список всех задач на главной странице

## Лицензия

MIT

